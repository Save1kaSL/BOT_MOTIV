# Запуск

## Запуск (RKO, /offers)

```bash
./bot.sh
```

Команды: `/offers` `/menu` `/start`

## Node бот (CRM, админ в боте)

```bash
./bot_node.sh
```

База Node-бота — **SQLite** `packages/db/prisma/bot.db`.
